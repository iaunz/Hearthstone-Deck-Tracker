<Window x:Class="BgsDataBridge.Settings.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BgsDataBridge"
        Height="340" Width="480"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterOwner">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <CheckBox x:Name="CbEnabled" Grid.Row="0" Content="Enabled" Margin="0,4"/>
    <TextBlock Grid.Row="1" Text="HTTP port" Margin="0,8,0,2"/>
    <TextBox  x:Name="TbPort" Grid.Row="2" Margin="0,0,0,8"/>
    <TextBlock Grid.Row="3" Text="Webhook URLs (one per line; events=* )" Margin="0,4,0,2"/>
    <TextBox  x:Name="TbWebhooks" Grid.Row="4" Height="120" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,0,0,8"/>
    <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Save"   Width="80" Margin="0,0,8,0" Click="OnSave"/>
      <Button Content="Cancel" Width="80" IsCancel="True"/>
    </StackPanel>
  </Grid>
</Window>
